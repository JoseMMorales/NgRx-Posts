import { PasswordSecureDirective } from './password-secure.directive';

describe('PasswordSecureDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordSecureDirective();
    expect(directive).toBeTruthy();
  });
});
