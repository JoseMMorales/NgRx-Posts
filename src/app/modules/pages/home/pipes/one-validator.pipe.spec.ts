import { OneValidatorPipe } from './one-validator.pipe';

describe('OneValidatorPipe', () => {
  it('create an instance', () => {
    const pipe = new OneValidatorPipe();
    expect(pipe).toBeTruthy();
  });
});
